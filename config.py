# ========================================
# 메롱 사용자 설정 파일 (GTA5 최적화)
# ========================================

# ========================================
# 메롱 정의
# ========================================
# 형식: '트리거 키':
# {'keys': ['실행할', '키', '순서'],
# 'delays': [딜레이1, 딜레이2, ...],
# 'holds': [홀드1, 홀드2, ...],
# 'mode': 모드}
# 
# mode 값:
#   0 = 비활성
#   1 = 누르고 있는 동안 연속 반복
#   2 = 한 번 누르면 1회만 실행
#
# GTA5 주의사항:
#   - delays: 최소 0.05초 권장 (안정성 향상)
#   - holds: 0.06초 이상 권장 (인식률 100%)
#   - 화살표 키(up/down/left/right)는 DirectInput 자동 적용
#   - 매크로끼리 연쇄 트리거 방지 적용됨 (안심하고 사용)

melongS = {
    # M 키 단축 (기본 예시)
    't': {
        'keys': ['m'],
        'delays': [0.05],
        'mode': 2
    },
    
    # Down 키 트리거 (GTA5 테스트 완료)
    'down': {
        'keys': ['9', '4'],
        'delays': [0.05, 0.05],
        'holds': [0.06, 0.06],
        'mode': 2
    },
    
    # 숫자 3 트리거
    '3': {
        'keys': ['9', '1'],
        'delays': [0.05, 0.05],
        'holds': [0.06, 0.06],
        'mode': 2
    },
    
    # 숫자 5: Down 키 4번 연속 (GTA5 테스트 완료)
    '5': {
        'keys': ['up', 'up', 'up', 'up'],
        'mode': 2
    },
    
    # 예시: K 키로 t, 1, 2 입력 (연쇄 방지 적용됨)
    'k': {
        'keys': ['t', '1', '2'],
        'delays': [0.05, 0.05, 0.05],
        'holds': [0.06, 0.06, 0.06],
        'mode': 2
    },
    
    # 예시: 연속 반복 (mode 1)
    # '6': {
    #     'keys': ['q', 'e'],
    #     'delays': [0.05, 0.05],
    #     'holds': [0.06, 0.06],
    #     'mode': 1
    # },
}

# ========================================
# 메롱 ON/OFF 토글 키
# ========================================
TOGGLE_KEY = '`'

# ========================================
# 실행 속도 설정 (GTA5 최적화)
# ========================================
# GTA5는 일반 프로그램보다 긴 딜레이 필요

# 키를 누르고 있는 시간 (초) - 기본값
# 개별 매크로에서 holds를 설정하지 않으면 이 값 사용
KEY_PRESS_DURATION = 0.06

# 키를 떼고 다음 키까지 대기 시간 (초) - 기본값
# 개별 매크로에서 delays를 설정하지 않으면 이 값 사용
KEY_RELEASE_DURATION = 0.02

# mode 1 루프 사이 딜레이 (초)
SEQUENCE_DELAY = 0.02

# ========================================
# 고급 설정 (건드리지 마세요)
# ========================================
# 매크로 연쇄 방지: 자동 활성화
# 매크로 실행 중에는 다른 매크로가 트리거되지 않음
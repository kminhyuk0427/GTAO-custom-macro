# ========================================
# 메롱 사용자 설정 파일
# ========================================
# 여기 파일 수정해서 커스텀 하셈

# ========================================
# 메롱 정의
# ========================================
# 형식: '트리거 키':
# {'keys': ['실행할', '키', '순서'],
# 'delays': [딜레이1, 딜레이2, ...],
# 'holds': [홀드1, 홀드2, ...],
# 'mode': 모드}
# 
# mode 값:
#   0 = 비활성 (메롱 존재하지만 동작하지 않음)
#   1 = 누르고 있는 동안 연속 반복 실행
#   2 = 한 번 누르면 시퀀스 1회만 실행 (꾹 눌러도 1회만)
#
# delays 값 (선택사항):
#   - 각 키 실행 후 대기할 시간 (초 단위)
#   - delays를 지정하지 않으면 기본값(KEY_RELEASE_DURATION) 사용
#   - delays 개수는 keys 개수와 같아야 함
#   - 그타에서는 최소 0.03초가 안전함(이 밑으로 내려가면 씹힐때가 있음)
#
# holds 값 (선택사항):
#   - 각 키를 누르고 있을 시간 (초 단위)
#   - holds를 지정하지 않으면 기본값(KEY_PRESS_DURATION) 사용
#   - holds 개수는 keys 개수와 같아야 함
#   - 0보다 큰 값을 지정하면 해당 시간동안 키를 누르고 있음
#
# 사용 가능한 키:
#   - 일반 키: 'a', 'b', '1', '2' 등
#   - 특수 키: 'space', 'enter', 'shift', 'ctrl', 'alt', 'tab' 등
#
# 예시 1: 기본 딜레이 사용
#   '1': {
#       'keys': ['q', 'w'],
#       'mode': 1
#   }
#   -> 1번 키를 누르고 있으면 q, w가 기본 딜레이로 연속 반복
#
# 예시 2: 개별 딜레이 지정
#   '2': {
#       'keys': ['2', '1'],
#       'delays': [0.5, 0.01],
#       'mode': 2
#   }
#   -> 2번 키를 누르면: 2 입력 → 0.5초 대기 → 1 입력 → 0.01초 대기
#
# 예시 3: 키 홀드(누르고 있기) 사용
#   't': {
#       'keys': ['s', 'd'],
#       'holds': [2.0, 0.01],  # s를 2초동안 누르고 있다가 뗀 후 d 입력
#       'delays': [0.1, 0.01],
#       'mode': 2
#   }
#
# 예시 4: 비활성 메롱
#   '3': {
#       'keys': ['a', 'b'],
#       'mode': 0  # 이 메롱은 동작하지 않음
#   }
#
melongS = {
    '1': {
        'keys': ['h', 'e', 'l', 'l', 'o'],
        'mode': 2
    },
}

# ========================================
# 메롱 ON/OFF 토글 키
# ========================================
TOGGLE_KEY = '`'

# ========================================
# 실행 속도 설정 (고급)
# ========================================
# 단위: 초 (1초 = 1.0, 10밀리초 = 0.01)

# 키를 누르고 있는 시간
KEY_PRESS_DURATION = 0.01

# 키를 떼고 다음 키까지 대기 시간
KEY_RELEASE_DURATION = 0.01

# 전체 시퀀스 완료 후 다음 반복까지 대기 시간
SEQUENCE_DELAY = 0.01  # mode 1 루프 사이 딜레이
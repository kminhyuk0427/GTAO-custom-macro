# GTA5M


##  주요 기능(세부 설명은 config.py)

-  **mode설정**: 0=비활성 1=연속동작작 2=단일동작
-  **holds설정**: 특정 키를 몇 초 동안 누르고 있도록 설정 가능
-  **토글 키 지정**: 기본은 백틱키("`" 또는 "~")로 되어있음

##  주의사항

-  **중복 X**: 메롱 진행중에는 다른 메롱이 눌리지 않음
-  **반복 중단**: 메롱 반복동작 중에 키를 때면 그 즉시 동작중이던 메롱이 중지됨
-  **반복 동작 오류1**: mode1(반복동작)에서 키를 매우 빠르게 눌렀다 때면 keyUP이 인식되지 않아 계속해서 입력될 수 있음
-  **반복 동작 오류2**: 반복동작 모드시 os키입력이 섞이는 문제 있음

##  실행법
### 1. 패키지 설치
```bash
pip install -r requirements.txt
```

### 2. 실행 파일 생성

-  다운로드 받은 폴더에서 build.bat를 실행하여 빌드

### 3. 메롱 실행

-  바탕화면에 아이콘 클릭 후 실행(관리자 권한 자동으로 들어감)
-  바탕화면에 없으면 ..\custom-melong\dist폴더에 실행 파일이 존재함
-  게임 키고 나서 메롱 실행을 권장

##  메롱 설정

### 기본 예시
```python
melongS = {
    '2': {
        'keys': ['3', '6'],
        'delays': [0.01, 0.01],
        'mode': 2
    },
}
```

### 홀드키 예
```python
't': {
    'keys': ['s', 'd'],
    'holds': [2.0, 0.01],  # s를 2초 동안 누름
    'delays': [0.1, 0.01],
    'mode': 2
}
```

### 반복 동작 예
```python
'3': {
    'keys': ['q', 'w', 'e'],
    'mode': 1 
}
```

##  종료하는 법
-  작업표시줄 숨김 아이콘에 메롱 있을꺼임, 우클릭 -> 종료

'

'

'

개인 커스텀 자유

참고 프로젝트: [Lester-Ver2.0]https://github.com/JU5TDIE/Lester-Ver2.0




ver - 1.6

수정사항
1. 안쓰는 코드 제거 및 간소화
2. 유사 코드 통합
3. 미사용 클래스 제거로 메모리 개선
4. 프로그램 아이콘 편경
5. 빌드 가이드 생성 및 예외처리
6. 안전 테스트 완료
7. 빌드 잔여파일 삭제

(인게임 테스트 미적용) - 확인 해야함
